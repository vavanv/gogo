import { createStyles } from '@mui/styles';

export const styles = () =>
  createStyles({
    grow: {
      flexGrow: 1,
      fontStyle: 'italic',
    },
    menuButton: {
      marginLeft: -12,
      marginRight: 20,
    },
    languageButton: {
      width: '48px',
    },
  });
